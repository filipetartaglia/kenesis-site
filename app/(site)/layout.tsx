/**
 * Layout for the public site route group.
 * Wraps all public-facing pages without altering the URL structure.
 */
export default function SiteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
