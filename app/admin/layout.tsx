/**
 * Layout para a área administrativa.
 * Será expandido nas próximas fases com autenticação, sidebar, etc.
 */
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
